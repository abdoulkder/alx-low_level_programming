#include "main.h"

/**
 * _abs - function thatreturn the absolute value of a
 *@a: character which the absolute value must be printed
 * Return: returns a if a is greater than 0 and -a otherwise
 */

int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
